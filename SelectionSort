package sorting;

import java.util.Arrays;

public class SelectionSortDemo {
    /*
Selection Sort is one of the simplest sorting algorithms in which we repeatedly find the minimum element
 (for ascending order) from the unsorted part of the array and put it at the beginning.

It divides the array into two parts:

Sorted part (left side)

Unsorted part (right side)
At each step, we select the smallest (or largest) element from the unsorted part and move it to the sorted part.

That’s why it’s called Selection Sort — because it selects the smallest (or largest) element each time.

 Algorithm -

Let’s say the array has n elements.

Steps:

Start from the first element (index i = 0).

Assume it is the minimum element.

Compare it with every other element in the unsorted part.

If you find a smaller element, update the minimum index.

After scanning the unsorted part, swap the minimum element with the first unsorted element.

Move the boundary of the sorted part one step ahead.

Repeat until the entire array is sorted.

 0   1  2    3   4    5   6
[5 , 8, 10 , 4 , 3 , 10 , 1]

min = 6

 0   1  2    3   4    5   6
[1 , 8, 10 , 4 , 3 , 10 , 5]
min = 4

 0   1  2    3   4    5   6
[1 , 3, 10 , 4 , 8 , 10 , 5]

[1 , 3, 4 , 10 , 8 , 10 , 5]

[1 , 3, 4 , 5 , 8 , 10 , 10]

time complexity : O(n^2)



     */

     public static void main(String[] args){

        int[] arr = {5 , 8, 10 , 4 , 3 , 10 , 1};
        System.out.println(Arrays.toString(arr));
        selectionSort(arr);
        System.out.println("After sorting : "+ Arrays.toString(arr));
            
     }


     public static void selectionSort(int[] arr){

        int n = arr.length;

        for(int i = 0 ; i<n-1; i++){
            int minIndex = i;

            for(int j = i+1; j<n;j++){
                if(arr[j] < arr[minIndex]){
                    minIndex = j ; 
                }
            }

            int temp = arr[minIndex];
            arr[minIndex] = arr[i];
            arr[i] = temp;





        }


     }
    
}
